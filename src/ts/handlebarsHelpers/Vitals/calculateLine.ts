import { StatHelpers } from "../../apps/helpers/StatHelpers";

export const calculateLine = function (vitality: number) {
  let result = "";
  result = "" + StatHelpers.calculateLine(vitality);
  return result;
};
