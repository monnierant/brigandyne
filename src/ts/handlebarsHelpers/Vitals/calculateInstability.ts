import { StatHelpers } from "../../apps/helpers/StatHelpers";

export const calculateInstability = function (composure: number) {
  let result = "";
  result = "" + StatHelpers.calculateInstability(composure);
  return result;
};
